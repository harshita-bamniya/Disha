// Thin re-export — the canonical implementation lives in shared/components/primitives/Input.
// New code should import from '@/shared/components/primitives/Input' directly.
export { default } from '@/shared/components/primitives/Input'
