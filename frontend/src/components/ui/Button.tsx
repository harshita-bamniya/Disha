// Thin re-export — the canonical implementation lives in shared/components/primitives/Button.
// New code should import from '@/shared/components/primitives/Button' directly.
export { default } from '@/shared/components/primitives/Button'
